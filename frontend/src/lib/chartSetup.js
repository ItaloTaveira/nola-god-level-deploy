// Intencionalmente vazio.
// A configuração do Chart.js deve ser feita APENAS via import único de 'chart.js/auto'
// em 'src/main.jsx' para evitar múltiplas instâncias no bundle.

// Mantido propositalmente para evitar imports quebrados no futuro.
